const API_URL = "https://dummyjson.com";

export const PRODUCTS_API_URL = `${API_URL}/products`;

export const CATEGORIES_API_URL = `${API_URL}/categories`;

export const ORDERS_API_URL = `${API_URL}/orders`;

export default API_URL;
